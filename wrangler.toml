name = "bible-songs"
main = "dist/worker.js"
compatibility_date = "2024-11-01"

[vars]
DISCORD_CLIENT_ID = "1535729840827670655"
REDIRECT_URI = "https://bible-songs.walusimbileon1.workers.dev"

# Secrets (set via `wrangler secret put` or dashboard):
#   DISCORD_CLIENT_SECRET  — Discord app client secret (never commit!)
